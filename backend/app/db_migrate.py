# This module is superseded by Alembic migrations (alembic/versions/).
# The logic it contained (adding public_id / short_code columns and
# backfilling existing rows) has been moved to migration 001_initial_schema.py.
# Kept for reference only — no longer called at startup.
